{
  "name": "project",
  "main": "src/main.ts",
  "compilerOptions": {
    "allowJs": true,
    "strict": true,
    "lib": ["deno.ns", "deno.window"],
    "jsx": "react"
  },
  "imports": {
    "std/": "https://deno.land/std@0.184.0/",
    "postgres": "https://deno.land/x/postgres@v0.17.0/mod.ts"
  },
  "tasks": {
    "dev": "deno run --watch main.ts",
    "test": "deno test --allow-env --allow-read --allow-net"
    "coverage": "rm -rf ./coverage && deno test --allow-env --allow-read --allow-net --coverage=coverage && deno coverage ./coverage"
  },
  "lint": {
    "rules": {
      "exclude": ["no-explicit-any"]
    }
  },
  "deno": {
    "version": "1.32.5",
    "allowNet": true,
    "allowRead": true,
    "allowEnv": true
  }
}
